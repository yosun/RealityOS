
Each update increment visible version indicator in UI
Deploy only after version increment.
after each deployment conclude with deployed with version number.
Always increment version number in `package.json` and UI components (`App.tsx`, `Layout.tsx`) before running deployment scripts.